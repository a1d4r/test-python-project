version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: test-python-project:latest
    volumes:
      - ./app:/app/app
volumes:
  db-data: {}
  cache-data: {}
